CREATE TABLE EMPLOYEE
(EMP_ID VARCHAR2(20),
 EMP_NAME VARCHAR2(20),
 EMP_DOB DATE,
 EMP_SALARY NUMBER,
 EMP_CITY VARCHAR2(20));
 
 INSERT INTO EMPLOYEE VALUES('E_110','SAMEEN', to_date('1-1-2010','DD-MM-YYYY'),20000,'DHAKA');
 
 
 
 select * from EMPLOYEE;


insert into EMPLOYEE
(EMP_ID, EMP_NAME,EMP_DOB,EMP_SALARY,EMP_CITY) values ('E2018', 'SAM', 20000); //Doesn't work